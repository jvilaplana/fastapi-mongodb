# FastAPI + MongoDB Atlas example

- python3 -m venv venv
- source venv/bin/activate
- pip install -r requirements.txt
- Add .env file with `MONGODB_URI="mongodb+srv://<user>:<password>@<mongodb-atlas-domain>"`
- Run with `uvicorn app:app --reload`
